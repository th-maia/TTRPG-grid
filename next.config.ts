import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false, //faz o codigo rodar duas vezes no desenvolvimento para observar irregularidades.
};

export default nextConfig;
